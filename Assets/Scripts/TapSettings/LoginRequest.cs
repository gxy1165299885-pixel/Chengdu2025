using TapSDK.Core;
using UnityEngine;

public class LoginRequest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        TapSDKData.Instance.Initialize();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
